type Props = {
  open: boolean;
  onOpenChange: (open: boolean) => void;
};

const AddDevicesToRackModal = ({ open, onOpenChange }: Props) => {
  return <div>AddDevicesToRackModal</div>;
};

export default AddDevicesToRackModal;