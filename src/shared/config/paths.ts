const ROOTS = {
  auth: '/auth',
};

export const PATHS = {
  root: '/',
  terms: '/terms',
  privacy: '/privacy',
  login: '/login',
  project: '/project',
  auth: {
    callback: `${ROOTS.auth}/callback`,
  },
};
