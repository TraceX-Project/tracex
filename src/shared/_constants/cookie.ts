export const COOKIE_NAME = {
  accessToken: 'accessToken',
  refreshToken: 'refreshToken',
};

export const TOKEN_MAXAGE = {
  accessToken: 15 * 60, // 15 mins
  refreshToken: 7 * 24 * 60 * 60, // 7 days
};
